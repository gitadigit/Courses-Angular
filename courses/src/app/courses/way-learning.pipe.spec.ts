import { WayLearningPipe } from './way-learning.pipe';

describe('WayLearningPipe', () => {
  it('create an instance', () => {
    const pipe = new WayLearningPipe();
    expect(pipe).toBeTruthy();
  });
});
